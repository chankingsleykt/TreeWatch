* [x] Fix the backend crashing so the frontend doesn't freeze automatically
* [ ] Handle the user drawing a rectangle, then moving it, should not run both
* [ ] Add mode for comparing past years
* [ ] Handle loading delays